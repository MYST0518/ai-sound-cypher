name: Scheduled Deploy to GitHub Pages

on:
  schedule:
    # 2月7日 1:00 JST = 2月6日 16:00 UTC
    - cron: '0 16 6 2 *'
  workflow_dispatch: # 手動実行も可能にする

jobs:
  deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: update-feb7
      
      - name: Merge to main
        run: |
          git config user.name "GitHub Actions Bot"
          git config user.email "actions@github.com"
          git checkout main
          git merge update-feb7 --no-ff -m "🎵 Update Vol.18 Setlist (2026.02.06)"
          git push origin main
